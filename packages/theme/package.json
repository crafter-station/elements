{
  "name": "@elements/theme",
  "version": "0.1.0",
  "private": true,
  "description": "Theme switcher components and registry",
  "scripts": {
    "dev": "echo 'No dev script for theme package'",
    "build": "echo 'No build script for theme package'",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0"
  }
}
